class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        n1=int(num1[0])
        for i in range(1,len(num1)):
            n1=n1*10+int(num1[i])
        n2=int(num2[0])
        for i in range(1,len(num2)):
            n2=n2*10+int(num2[i])
        val=n1*n2
        return str(val)
        

